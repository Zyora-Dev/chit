name: Build zChit Owner Android APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "admin-mobile/**"
      - ".github/workflows/build-zchit-owner-android.yml"

concurrency:
  group: zchit-owner-android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-apk:
    name: Build Owner release APK
    runs-on: ubuntu-latest
    env:
      EXPO_PUBLIC_API_URL: ${{ vars.EXPO_PUBLIC_API_URL }}
    defaults:
      run:
        working-directory: admin-mobile

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: admin-mobile/package-lock.json

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Validate production API URL
        run: |
          if [ -z "$EXPO_PUBLIC_API_URL" ]; then
            echo "Set the GitHub repository variable EXPO_PUBLIC_API_URL before building."
            exit 1
          fi
          case "$EXPO_PUBLIC_API_URL" in
            http://*|https://*) ;;
            *) echo "EXPO_PUBLIC_API_URL must begin with http:// or https://"; exit 1 ;;
          esac

      - name: Verify exact Owner artwork
        run: |
          test "$(sha256sum assets/zchit-owner.png | cut -d ' ' -f 1)" = "39eca9774f56d19be70efa8f99e7519b73cf81c487fab3382f0bda36900decd7"

      - name: Type-check Owner app
        run: npx tsc --noEmit

      - name: Generate Android project
        run: npx expo prebuild --platform android --clean --no-install

      - name: Build Owner release APK
        working-directory: admin-mobile/android
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload zChit Owner APK
        uses: actions/upload-artifact@v4
        with:
          name: zChit-Owner-Android-APK
          path: admin-mobile/android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
          retention-days: 30