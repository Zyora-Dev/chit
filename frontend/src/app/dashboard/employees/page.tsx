"use client";
import Link from "next/link";import {FormEvent,useEffect,useState} from "react";import {BriefcaseBusiness,Plus,Search,ShieldCheck,UsersRound} from "lucide-react";import {Badge} from "@/components/ui/badge";import {Button} from "@/components/ui/button";import {Card,CardContent} from "@/components/ui/card";import {Input} from "@/components/ui/input";import {authenticatedApiRequest} from "@/lib/api";
type Employee={id:number;employee_code:string;full_name:string;mobile_number:string;department:string;designation:string;employment_type:string;status:string;kyc_status:string;collection_agent_enabled:boolean;is_active:boolean;joining_date:string};
export default function EmployeesPage(){const [items,setItems]=useState<Employee[]>([]);const [total,setTotal]=useState(0);const [search,setSearch]=useState("");const [loading,setLoading]=useState(true);const [error,setError]=useState("");async function load(term=""){setLoading(true);try{const data=await authenticatedApiRequest<{items:Employee[];total:number}>(`/api/v1/employees?page_size=100${term?`&search=${encodeURIComponent(term)}`:""}`);setItems(data.items);setTotal(data.total);}catch(err){setError(err instanceof Error?err.message:"Unable to load employees.");}finally{setLoading(false);}}useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer);},[]);function submit(event:FormEvent){event.preventDefault();void load(search);}const verified=items.filter(item=>item.kyc_status==="verified").length;const agents=items.filter(item=>item.collection_agent_enabled).length;return <div className="mx-auto max-w-[1600px] space-y-4"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-emerald-700">Workforce</p><h1 className="mt-1.5 text-2xl font-bold text-slate-950">Employees & payroll setup</h1><p className="mt-1 text-[13px] text-slate-600">Employee master, employment, KYC, statutory, bank, salary, and documents.</p></div><Button nativeButton={false} render={<Link href="/dashboard/employees/new"/>} className="bg-emerald-600 text-white"><Plus className="size-4"/>Add employee</Button></div><div className="grid gap-3 sm:grid-cols-3"><Stat icon={UsersRound} label="Active employees" value={total}/><Stat icon={ShieldCheck} label="KYC verified" value={verified}/><Stat icon={BriefcaseBusiness} label="Collection agents" value={agents}/></div><Card className="border border-slate-200 bg-white py-0 ring-0"><CardContent className="p-4"><form onSubmit={submit} className="flex gap-2"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500"/><Input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search name, code, mobile, designation" className="h-9 pl-9"/></div><Button type="submit" variant="outline">Search</Button></form></CardContent></Card>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Card className="overflow-hidden border border-slate-200 bg-white py-0 ring-0"><CardContent className="overflow-x-auto p-0">{loading?<p className="p-10 text-center text-sm text-slate-600">Loading employees…</p>:items.length===0?<p className="p-10 text-center text-sm text-slate-600">No employees found.</p>:<table className="w-full min-w-[1000px] text-xs"><thead className="bg-slate-50"><tr>{["Employee","Department / designation","Employment","Joining date","KYC","Agent","Status","Action"].map(label=><th key={label} className="px-4 py-3 text-left text-[10px] text-slate-700">{label}</th>)}</tr></thead><tbody>{items.map(item=><tr key={item.id} className="border-t hover:bg-emerald-50"><td className="px-4 py-3"><p className="font-bold text-slate-950">{item.full_name}</p><p className="text-[9px] text-slate-500">{item.employee_code} · {item.mobile_number}</p></td><td className="px-4 py-3"><p className="font-semibold">{item.department}</p><p className="text-[9px] text-slate-500">{item.designation}</p></td><td className="px-4 py-3">{item.employment_type.replaceAll("_"," ")}</td><td className="px-4 py-3">{new Date(`${item.joining_date}T00:00:00`).toLocaleDateString("en-IN")}</td><td className="px-4 py-3"><Badge className={item.kyc_status==="verified"?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-700"}>{item.kyc_status}</Badge></td><td className="px-4 py-3">{item.collection_agent_enabled?"Enabled":"No"}</td><td className="px-4 py-3"><Badge variant="outline">{item.status}</Badge></td><td className="px-4 py-3"><Button nativeButton={false} variant="outline" size="sm" render={<Link href={`/dashboard/employees/${item.id}`}/>}>View</Button></td></tr>)}</tbody></table>}</CardContent></Card></div>}
function Stat({icon:Icon,label,value}:{icon:typeof UsersRound;label:string;value:number}){return <Card className="border border-slate-200 bg-white py-0 ring-0"><CardContent className="flex items-center gap-3 p-4"><Icon className="size-5 text-emerald-700"/><div><p className="text-xs text-slate-600">{label}</p><p className="text-xl font-bold">{value}</p></div></CardContent></Card>}
